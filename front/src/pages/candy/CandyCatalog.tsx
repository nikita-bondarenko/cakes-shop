import React from 'react';
import Catalog from "../../components/catalog/Catalog";

const CandyCatalog = () => {
    return (
        <Catalog title={"Каталог сладостей"} typeId={3}></Catalog>
    );
};

export default CandyCatalog;